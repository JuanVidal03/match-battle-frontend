export const removeCeroToANumber = (number) => {
    console.log(number);
    const numsArray = number.split("");
    const removeCero = numsArray[0]
    console.log(removeCero);
    // const finalNumber = parseInt(removeCero);
    // console.log(finalNumber, removeCero);
    // return finalNumber;

}